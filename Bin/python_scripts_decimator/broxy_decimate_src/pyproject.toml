[build-system]
requires = ["setuptools>=64", "wheel", "pybind11"]
build-backend = "setuptools.build_meta"

[project]
name = "broxy"
version = "0.1.0"
description = "Broxy decimator bindings"
readme = "README.md"
requires-python = ">=3.7"
license = {text = "GPL-3.0-or-later"}
authors = [{name = "johnb"}]

[tool.setuptools]
packages = []
